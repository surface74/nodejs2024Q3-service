services:
  # client:
  #   build: ../simple-client
  #   environment:
  #     PORT: ${PORT}
  #   networks:
  #     - frontend
  #   depends_on:
  #     - srv

  srv:
    build: .
    restart: always
    ports:
      - ${PORT}:${PORT}
    environment:
      - PORT=${PORT}
    networks:
      - frontend
    volumes:
      - ./postgres-data:/usr/app/postgres-data/

networks:
  frontend:
    driver: bridge
